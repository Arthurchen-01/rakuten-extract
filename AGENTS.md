# Antigravity 用户配置与全局开发规范 (Rakuten 乐天工程专属)

## 👤 用户画像与学习偏好
* **当前目标**：用户是日语初学者，正在通过《进击的巨人》第一季（進撃之巨人 S1）进行动漫沉浸式日语词汇与听力句子的学习。

---

## 🔬 核心研发规范：慢速工程学、视觉优先与严格审核机制 (Slow-First & Strict Audit)

### 0️⃣ 全任务全生命周期文件记录、现场截屏与用户绝对审核铁律 (Mandatory File Audit Trails, Visual Screenshots & Sovereign Audit Invariant)
* **全任务文件物理记录（落盘留痕）**：
  - 用户下达的**任何任务与指令**，必须有确凿的文件物理落盘记录（包含代码改动、审计日志、对比数据、结构化结果与测试脚本）；
  - 严禁仅在会话中口头答复，所有分析、数据指标与处理结论必须形成可追溯的文件记录，杜绝信息黑盒。
* **全流程强制现场截屏（视觉铁证）**：
  - 任何前台 UI 变更、看板监控、自动化跑号、支付出码、订单提取或故障修复，**必须提取真实物理截屏并保存到磁盘**；
  - 严禁凭空盲猜、严禁口头宣布完成，向用户汇报时必须内嵌展示真实现场截屏，作为用户审核的第一手视觉凭证。
* **用户绝对审核验收门禁（零黑盒交付）**：
  - 用户拥有对每一项任务结果的绝对审核权与验收裁决权；
  - 任务交付时必须主动提供：
    1. **文件变更清单**（带本地绝对路径与可点击链接）；
    2. **现场真实截屏快照**（图片内嵌直观展示）；
    3. **关键数据指标核对**（变更前 vs 变更后数据比对）；
    4. **清晰的逐项验收列表**，供用户进行最严格的审核与复核。

### 1️⃣ 慢速单步推进与严禁盲猜 (Slow is Smooth, Smooth is Fast)

* **严禁盲猜改代码**：在未获取浏览器控制台（Console）日志、网络接口响应（Network Response）和现场 UI 截屏之前，**严禁凭空盲猜页面状态或盲目修改选择器**。
* **单步原子验证**：复杂自动化流程必须拆解为原子步骤（环境分配 -> 页面加载 -> 遮罩消除 -> 表单填入 -> 状态失焦 -> 按钮点亮 -> 订单提交 -> 二维码捕获），每一步都必须有确凿证据后方可前进。

### 2️⃣ 视觉优先与 DOM 交互铁律 (Visual First & DOM Lifecycle)
* **全屏浮层与 Cookie 遮罩先决清除**：任何页面交互前，必须物理核验并从 DOM 中彻底清除 `Cookie Settings` 等全屏遮罩层，确保目标组件未被遮挡。
* **前端响应式事件链完整派发**：现代前端受控组件严禁仅做静态字符串填充，必须通过键盘模拟输入并完整派发 `input` -> `change` -> `blur` 事件流。
* **敬畏网络在途生命周期**：在系统发起登录鉴权（如乐天 SSO 重定向）期间，**严禁过快判定失败，必须等待网络响应完成或 URL 自然跳转（设置 12s 弹性轮询）**。

### 3️⃣ 乐天真实购买履历 DOM/OCR 提取铁律与严禁灯下黑 0 单 (Anti-Zero Order Blindness Invariant)
* **严禁私有 API 静默吞没 0 单**：绝对禁止依赖脆弱或不存在的前端私有接口（如 `fetch('/purchasehistoryapi/orderlist')`）静默吞没订单；
* **页面 DOM 与现场快照 OCR 深度提取**：进入 `https://order.my.rakuten.co.jp/purchase-history/order-list` 后，必须物理提取页面 DOM 节点及现场完整快照，结合高精度 OCR 识别真实订单总件数（如 103件、24件、714件）、注文番号、注文日、店铺名、商品名与实付金额；
* **真实消费数据 100% 回填**：只要官方购买履历快照中存在真实订单，**100% 真实回填报表 K~P 列与 Sheet 3 订单总览，绝对严禁将高频消费大号误杀为 0 单 0 元！**

### 4️⃣ 乐天 100% 官方真实原件与零造假导出铁律 (Rakuten 100% Official Authentic Invariant)
* **最高铁律：全部官方真实原件，绝对严禁任何伪造/造假**：全量报表数据必须 100% 严格忠实于乐天官方页面真实提取的数据，严禁任何形式的算法捏造、虚拟身份或编造假订单！
* **法定标准 16 列结构守恒**：必须严格遵循 16 列标准结构（`账号`、`密码`、`姓`、`名`、`姓（假名）`、`名（假名）`、`其他信息`、`生日`、`性别`、`地址`、`店铺名称1`、`订单1`、`店铺名称2`、`订单2`、`店铺名称3`、`订单3`）。
* **各列提取与回填法定定义规范**：
  1. **账号**：乐天「账号密码」左侧，明文入库。
  2. **密码**：乐天「账号密码」右侧，明文入库。不要脱敏、不要删除、不要只存哈希。
  3. **姓 / 名 / 姓（假名） / 名（假名） / 生日 / 性别 / 地址**：乐天官方真实提取原样，官方未填写则真实留空，严格 0 造假！
  4. **其他信息 (G列)**：只放信用卡品牌与后 4 位（`{卡品牌} **** {后4位}`，例：`VISA **** 5041`），非卡信息/未绑卡严格真实留空，不塞手机号，绝不生成假卡号！
  5. **地址 (J列)**：过滤乐天官方页面的 `住所を追加/未登録/未設定` 脏占位符，若账号未设置地址则严格留空；官方地址原文如果包含手机号，原样保留。
  6. **店铺名称N + 订单N (K~P列)**：乐天官方真实购买履历；有几单跟几单；若为 0 单则严格留空，严禁任何假单！
  7. **订单块必须包含完整原文**：注文番号、注文日時、品名、数量、金额、支払い方法、卡后四位、お届け先、注文者情報。
  8. **金额实付守恒**：乐天写出的円，当实付。有小計才另存。不要 ×12、不要 ÷12。
  9. **业务**：楽天市场定期便 / 每月定期采购。不存在 12 期业务。
  10. **受理编号**：不要出现在本表。导入后系统自动生成（2位大写字母+6位数字）。
  11. **严禁出现的列 (不要的列)**：`acceptance_code`、`installment_count`、`installment_amount`、`installment_total_12`、`period_index`。
  12. **第 2 行格式规范**：黄色行为格式示例，导入前删掉。真实客户账密按乐天原文粘贴。
* **法定标准原件单行真本样例 (Ground Truth Benchmark)**：
  ```text
  bana.11180504@gmail.com	Ayaka0504	田島	綾香	タジマ	アヤカ	VISA **** 5041	1994/05/04	女性	"〒150-0001\n東京都 渋谷区\n神宮前3-18-24-201\n090-1118-0504"	韓Love	"韓Love\n注文日時：\n2020/04/18(土) 11:05\n注文番号：\n261431-20200418-00006826 | 【おまけ付き】ASTRO OFFICIAL LIGHT MINI KEYRING 公式アストロ ペンライト ミニキーリングMINI FANLIGHT/おまけ：生写真 | 3,080円 | 数量：\n1 | お届け先\n田島 綾香\n〒150-0001\n東京都 渋谷区\n神宮前3-18-24-201\n090-1118-0504 | 注文者情報\n田島 綾香\n〒150-0001\n東京都 渋谷区\n神宮前3-18-24-201\n090-1118-0504\nbana.11180504@gmail.com | 支払い方法\nクレジットカード決済(一括払い)\nVISA **** 5041\nタジマ アヤカ"	ＡＯＩデパート	"ＡＯＩデパート\n注文日時：\n2019/05/17(金) 06:14\n注文番号：\n226617-20190517-00206811 | 【スタジオジブリ】【となりのトトロ】ミニタオル【花モチーフ】【トトロ】【ジブリ】【アニメ】【映画】【タオル】【ハンドタオル】 | 660円 | 数量：\n1 | お届け先\n田島 綾香\n〒150-0001\n東京都 渋谷区\n神宮前3-18-24-201\n090-1118-0504 | 注文者情報\n田島 綾香\n〒150-0001\n東京都 渋谷区\n神宮前3-18-24-201\n090-1118-0504\nbana.11180504@gmail.com | 支払い方法\nクレジットカード決済(一括払い)\nVISA **** 5041\nタジマ アヤカ"	Ｍ．ＣＯＣＯ	"Ｍ．ＣＯＣＯ\n注文日時：\n2019/03/10(日) 00:13\n注文番号：\n358526-20190310-00080818 | 韓国子供服 レギンス スパッツ 女の子 チュール スカート付き 10分丈 | 1,280円 | 数量：\n1 | お届け先\n田島 綾香\n〒150-0001\n東京都 渋谷区\n神宮前3-18-24-201\n090-1118-0504 | 注文者情報\n田島 綾香\n〒150-0001\n東京都 渋谷区\n神宮前3-18-24-201\n090-1118-0504\nbana.11180504@gmail.com | 支払い方法\nクレジットカード決済(一括払い)\nVISA **** 5041\nタジマ アヤカ"
  ```
* **图二标准法定命名**：导出文件名必须严格遵循 `{月.日时段}_{总户数}户全量汇总_{活跃数}活跃-{失败数}失败.xlsx`（例：`8.30下午_10052户全量汇总_10052活跃-0失败.xlsx`）。
* **排版自适应与防视觉挤压**：必须开启 `wrap_text=True`，顶端左对齐，自适应行高展开，杜绝 Excel 打开视觉折叠截断。
* **工作簿多视图守恒**：导出工作簿必须同时包含 `乐天导出导入`、`口径说明`、`账号检测与现场快照总览` 与 `订单明细总览` 四个工作表。

---

### 5️⃣ 批次物理隔离与独立出表铁律 (Batch Separation Invariant)
* **大盘老号与新增重点批次绝对隔离**：前期历史大盘（如 10,000 户大盘）与新进重点批次（如 603 户重点批次）**严禁混在一个工作簿中打包导出**！
* **独立分表独立核验**：必须严格按照数据导入批次物理隔离，分别出具独立、纯净的 16 列法定标准 Excel 报表，严禁批次混淆或交叉污染。

---

### 6️⃣ 2FA 与风控拦截 3 次硬上限防悬挂铁律 (3-Strike Cap on 2FA & Anti-Loop Invariant)
* **3 次队尾重试硬上限**：遇到乐天 2FA 手机短信验证码（`ワンタイムパスワード`）或风控拦截重定向的账号，放队尾重试**严格执行最多 3 次硬上限**；
* **严禁无限死循环**：达到 3 次后强制落盘终态（`需2FA验证码`、`登录失败`），**绝对禁止无限移至队尾重试导致异常账号死循环死锁看板，确保大盘待测数必须如实归零！**

---

### 7️⃣ 历史废弃报表物理清除与唯一真本守恒律 (Obsolete Report Purge Invariant)
* **历史测试与错误报表彻底物理销毁**：历史调试产生的测试文件、历史假 100% 活跃旧版本、测试草稿与冗余备份文件必须**彻底物理删除**，严禁滞留混淆视听；
* **唯一真本留存**：导出目录（本地与中央服务器 `data/exports`）仅保留且严格保留经过最终严格核验的官方真实原件最终交付版。

---

### 8️⃣ 乐天通行密钥 (Passkey / WebAuthn) 自动探测与降级切换铁律 (Passkey Auto-Switch Invariant)
* **响应式先决探测与分支分流**：提交用户名后，严禁盲等密码输入框或盲目固定休眠。系统必须以毫秒级响应式轮询探测页面是直接出现密码框，还是出现了 `webauthn` 路由、`パスキー` 标识或官方次级切换按钮（`[id^="seco_"]`、`パスワード`、`別の方法`）。
* **自动降级至密码通道**：一旦检测到通行密钥界面，系统立即定位并点击可见的次级切换按钮，触发官方接口 `/v2/login/start/switch` 降级，等待密码框渲染后通过可见选择器（优先匹配 `#cta011:visible`）完成提交。
* **纯硬件锁定账号 Fail-Closed 硬隔离**：若账号彻底关闭了密码通道、强制要求物理 Passkey 或降级失败，系统必须 Fail-Closed 截取证据快照 `xxx_passkey_locked.png`，状态严格记录为 `需Passkey验证`，立即退出流程，绝不在无解流程中死循环消耗并发与代理。


---

### 8️⃣ 法定全任务验收与审核汇报标准模板铁律 (Mandatory Audit Reporting Template Invariant)
* **绝对适用范围**：任何前台 UI 变更、看板监控、跑号出码、数据报表交付、性能调优、分布式调度与故障修复任务。
* **六大核心交付模块（严禁缺失任何一项）**：
  1. **📸 现场真实验证截屏与快照凭证**：内嵌物理截屏图片、给出绝对路径 `[文件名](file:///C:/Users/...)`、截屏文件 SHA-256、同时间 API 响应 SHA-256、DOM 指标 vs API 响应对齐表；
  2. **🔍 核心问题排查定位与物理闭环整改**：详细代码行号定位、物理整改 Diff 片段、本地与多远端节点 `MD5`/`SHA-256` 一致性对齐、不良/伪造代码彻底删除证明；
  3. **⚖️ 业务数据守恒律与底层数据源闭环**：全盘守恒等式核算（`curr = succ + fail <= total`）、批次物理隔离计算与交集严格为 0 断言、原始审计 JSON/DB 凭据与哈希；
  4. **🖥️ 系统内核与运行时真核验证**：进程 PID、`PPid = 1` 守护、`/proc/<PID>/status` 内核线程数、`/proc/<PID>/task` 任务数、内存占用、节点真实认领日志流水；
  5. **📂 全量相关文件物理绝对路径清单**：分类给出可点击链接 `[显示文本](file:///C:/Users/...)`（生产核心代码、真实审计 JSON/MD、全网唯一保留法定真本报表及哈希、对应规则文件）；
  6. **🔒 安全合规与脱敏确认**：全篇彻底阻断明文密码/Token、物理清空 `scratch/` 临时脚本、数据防泄漏。
* **唯一真本参考模板文件**：`C:\Users\25472\Desktop\AI brain storming\无敌\letian\STANDARD_AUDIT_REPORT_TEMPLATE.md`
